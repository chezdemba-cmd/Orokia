{
  "buildCommand": "pnpm --filter @orokia/shared build && pnpm --filter @orokia/web build && pnpm run build:api",
  "outputDirectory": "packages/web/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "crons": [{ "path": "/api/internal/cron/escalations", "schedule": "0 0 * * *" }]
}
